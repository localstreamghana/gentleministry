name: gentle_church
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: ^3.10.1


dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  go_router: ^17.0.1
  riverpod: ^3.0.3
  firebase_core: ^4.3.0
  firebase_auth: ^6.1.3
  cloud_firestore: ^6.1.1
  firebase_storage: ^13.0.6
  firebase_messaging: ^16.1.0 # Push notifications
  video_player: ^2.8.0        # Sermon videos
  youtube_player_flutter: ^9.0.0  # YouTube integration (great for live/streamed sermons)
  chewie: ^1.7.0              # Better video controls
  audio_video_progress_bar: ^2.0.0
  just_audio: ^0.10.5          # Audio sermons
  cached_network_image: ^3.3.0
  intl: ^0.20.2               # Dates & formatting
  flutter_local_notifications: ^20.0.0
  google_maps_flutter: ^2.7.0 # Church location
  url_launcher: ^6.3.0        # Links & giving
  in_app_purchase: ^3.2.0     # If donations via app
  hive: ^2.2.3                # Local storage for offline Bible/bookmarks (or sqflite)
  flutter_riverpod: ^3.0.3
  riverpod_annotation: ^3.0.3
  google_fonts: ^6.3.3
  google_sign_in: ^7.2.0
  pin_code_fields: ^8.0.1
  firebase_app_check: ^0.4.1+3
  email_otp: ^3.1.0
  app_links: ^7.0.0
  shared_preferences: ^2.5.4
  share_plus: ^12.0.1
  timezone: ^0.10.1
  image_picker: ^1.2.1
  crypto: ^3.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  build_runner: ^2.10.4
  riverpod_generator: ^3.0.3

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

 
  assets:
   - assets/bible/kjv.json

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
